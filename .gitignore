# Raw GitHub API dumps — regenerated by refresh.sh, too large to track.
data/prs.json
data/stars.json
data/commits.json
data/forks.json
data/issues.json
data/stargazer_profiles.json

# Logs
data/refresh.log
data/snapshots.log

# Competitor raw star history (large, regeneratable via fetch_competitor_stars.js)
data/competitors/*.jsonl
data/competitors/*.cursor
data/competitors/*.log
# Keep data/competitors/headlines.json + data/competitors/metrics.json

# Keep data/metrics.json (small, drives the dashboard build)
# Keep data/snapshots.jsonl (append-only history — the whole point of snapshotting)

.DS_Store
node_modules/
.claude/
