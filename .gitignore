# Raw GitHub API dumps — regenerated by refresh.sh, too large to track.
data/prs.json
data/stars.json
data/commits.json
data/forks.json
data/issues.json
data/stargazer_profiles.json

# Logs
data/refresh.log
data/snapshots.log

# Competitor backfill state (regenerated/resumed across runs)
data/competitors/*.cursor
data/competitors/*.log
# Keep JSONL files committed — CI does incremental updates and needs prior history

# Keep data/metrics.json (small, drives the dashboard build)
# Keep data/snapshots.jsonl (append-only history — the whole point of snapshotting)

.DS_Store
node_modules/
.claude/
